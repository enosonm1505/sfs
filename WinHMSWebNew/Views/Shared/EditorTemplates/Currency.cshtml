@using Kendo.Mvc.UI

@(Html.Kendo().DropDownList()
              .Name("Currency")
            .DataTextField("Text")
            .DataValueField("Value")    
            .Height(300)
                  .Events(e => e.Change("dropchange"))
      .DataSource(d => d.
                      Read(r => r.Action("AreaCurrencyLoad", "SalesAndMarket")
          )
          )
)


