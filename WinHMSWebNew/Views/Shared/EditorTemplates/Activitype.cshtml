@using Kendo.Mvc.UI

@(Html.Kendo().DropDownList()
          .Name("Activitype")        
            .DataTextField("Text")
            .DataValueField("Value")
                 .Events(e => e.Change("dropchange")) 
                    
      .DataSource(d => d.
          Read(r => r.Action("ActvMstDropopen", "SalesAndMarket")
          )
          )
)

 
        